# Training Companion Lakehouse — 고정 스택
# Iceberg 1.11.0 이 Spark 4.1 까지만 런타임을 제공하므로 PySpark 는 4.1.x 로 고정한다.
# (PySpark 4.2 를 쓰면 iceberg-spark-runtime 매칭 아티팩트가 없음)
pyspark==4.1.3

# Gold 레이어
dbt-core==1.12.5
dbt-spark[session]

# Spark <-> pandas 변환 (dbt-spark session, 검증 스크립트)
pandas
pyarrow
